export interface Clients {
  firstName: string;
  lastName: string;
  age: string;
  address: string;
}

export interface ShowClients {
  id: string;
  firstName: string;
  lastName: string;
  age: string;
  address: string;
}
