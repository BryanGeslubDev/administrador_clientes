import { Image } from "react-native";
import phone from "./phone.png";
import success from "./success.png";
import sync from "./sync.png";
export const phoneUri = Image.resolveAssetSource(phone).uri;
export const successUri = Image.resolveAssetSource(success).uri;
export const syncUri = Image.resolveAssetSource(sync).uri;
